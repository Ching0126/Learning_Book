

# 学习笔记

## C++11新特性

### 1. 智能指针

解决问题： 独占/共享所有权指针的释放、传输

#### unique_ptr（独占指针）

- 在任何给定时刻，只能有一个指针管理内存
- 指针超出作用域，内存自动释放
- 不可copy,只可以move
  - 在做函数参数或是返回值中一定要注意所有权
  - **move调用结束之后，原指针销毁，所有权交给新指针。**

**三种创建方式：**

- 通过已有裸指针创建

  ```c++
  //裸指针改变，独占指针值会随之变化，相当于浅拷贝。 
   Cat *c_p2 = new Cat("yz");
      unique_ptr<Cat> u_c_p2{c_p2};
  ```

- new创建

  ```c++
   unique_ptr<Cat> u_c_p3{new Cat("dd")};
  u_c_p3->cat_info();
  ```

- std::make_unique创建(官方推荐)

  ```c++
   unique_ptr<Cat> u_c_p4 = make_unique<Cat>();
      u_c_p4->cat_info();
  ```

**通过get()获取地址**

```
 cout<<"cat adress"<<u_c_p3.get()<<endl;
```

**实现了->与***

```c++
unique_ptr<int> u_i_p3{new int(100)};   
u_c_p4->set_name("123");
cout<<*u_i_p3<<endl;
```

#### share_ptr（计数指针/共享指针）

- 与unique_ptr不同，其可共享
- 每copy一次，count+1；destory一次，count-1;
- 销毁只销毁一次，所有指针都为0，才会销毁。

#### 区别和联系

- shared_ptr不可转化为unique_ptr;

- unique_ptr可以转化为shared_ptr;(move())

  ```c++
   unique_ptr<Cat> cp1 = make_unique<Cat>("dd");
  shared_ptr<Cat> cp2 = move(cp1);
  shared_ptr cp3 = get_unique_ptr();//返回值为unique，自动转化。
  ```

- 返回值尽量返回unique_ptr;

#### weak_ptr(弱引用)

不可以调用->和*

解决循环依赖问题

weak_ptr可以通过lock()提升为shared_ptr；

```c++
shared_ptr<Cat> cp2 = wc1.lock();
```

weak_ptr可以调用shared_ptr,但不会导致shared_ptr+1；

```c++
shared_ptr<Cat> cp1 = make_shared<Cat>("c1");
    weak_ptr<Cat> wc1(cp1);
    cout<<cp1.use_count()<<endl;
    cout<<wc1.use_count()<<endl;
```

### 2. nullptr

在二叉树查询时，

​	如果不将结点设置为nullptr，在进行遍历时，系统无法找到该结点的位置；

​	若将结点设置为nullptr，则系统可以找到该结点，知识该结点为空。

NULL为一个宏定义  define null 0

nullptr相当于一个C++关键字

### 3. 基于范围的for循环

```c++
for(range_declaration:range_expression) loop_statement
```

```c++
map<int,string> index_map{{1,"hello"},{2,"world"},{3,"hello"}};
int int_array[]={0,1,2,3,4,5};

int main() {
    for (const auto &e : index_map) {
        cout<<e.first<<" "<<e.second<<endl;
    }
   for (auto e:int_array) {
       cout<<e<<endl;
   }

    return 0;
}
```

![image-20260107173432203](C:/Users/qiyi/AppData/Roaming/Typora/typora-user-images/image-20260107173432203.png)

### 4. ind

## Git基础

学习网站：learngitbranching.js.org

git commit 提交任务，只提交修改的部分。

git branch newImage 创建一个叫newImage的分支

git branch -d name 检查后删除

git branch -D name 强制删除

git checkout name 切换到name分支

git checkout -b name新建并且切换到name分支。

git status 查看状态

git log 查看日志

git reset --hard commitID

创建一个新的分支同时切换到新创建的分支的话，可以通过 `git checkout -b <your-branch-name>`

第二种合并分支的方法是 `git rebase`。Rebase 实际上就是取出一系列的提交记录，“复制”它们，然后在另外一个地方逐个的放下去。

我将常见的Git命令按**功能分类**整理成表格，包含命令、含义说明，方便你快速查阅：

| 功能分类       | Git命令                          | 含义说明                                                     |
| -------------- | -------------------------------- | ------------------------------------------------------------ |
| **仓库初始化** | `git init`                       | 在当前目录初始化一个新的Git仓库（创建.git隐藏目录）          |
| **仓库克隆**   | `git clone 仓库地址`             | 从远程仓库（如GitHub）克隆代码到本地（会创建对应目录）       |
| **文件操作**   | `git add 文件名`                 | 将指定文件添加到暂存区（准备提交）；`git add .`添加当前目录所有修改文件 |
|                | `git commit -m "提交信息"`       | 将暂存区的文件提交到本地仓库，并附带提交说明                 |
| **状态查看**   | `git status`                     | 查看当前工作区、暂存区的文件状态（哪些文件被修改/未跟踪）    |
|                | `git log`                        | 查看提交历史（显示所有commit记录，含作者、时间、提交信息）   |
|                | `git log --oneline`              | 简化版提交历史（每行显示一个commit的哈希和信息）             |
| **分支操作**   | `git branch`                     | 查看本地所有分支（当前分支前会标`*`）                        |
|                | `git branch 分支名`              | 创建新的本地分支                                             |
|                | `git checkout 分支名`            | 切换到指定分支；`git checkout -b 分支名`创建并切换到新分支   |
|                | `git merge 分支名`               | 将指定分支的代码合并到当前分支                               |
|                | `git branch -d 分支名`           | 删除指定本地分支（需先切换到其他分支）                       |
| **远程仓库**   | `git remote add origin 仓库地址` | 关联本地仓库到远程仓库（通常命名为origin）                   |
|                | `git push -u origin 分支名`      | 将本地分支代码推送到远程仓库；`-u`绑定本地与远程分支（后续可直接`git push`） |
|                | `git pull`                       | 拉取远程仓库的最新代码并合并到当前本地分支（等价于`git fetch + git merge`） |
| **版本回退**   | `git reset --hard 提交哈希`      | 回退到指定commit版本（会覆盖工作区和暂存区的修改，需谨慎）   |
|                | `git revert 提交哈希`            | 生成一个新的commit，撤销指定commit的修改（不会覆盖历史，更安全） |
| **暂存区操作** | `git restore 文件名`             | 丢弃工作区中指定文件的修改（恢复到暂存区/仓库的版本）        |
|                | `git reset HEAD 文件名`          | 将暂存区的指定文件撤销到工作区（取消`git add`的操作）        |

## STL

### 表格1：STL 常用容器核心成员函数
| 容器类型   | 核心成员函数                 | 函数作用                                                     |
| ---------- | ---------------------------- | ------------------------------------------------------------ |
| **vector** | `push_back(val)`             | 在容器尾部添加元素 `val`                                     |
|            | `pop_back()`                 | 删除容器尾部元素                                             |
|            | `size()`                     | 返回容器中元素个数                                           |
|            | `empty()`                    | 判断容器是否为空（空返回 true）                              |
|            | `clear()`                    | 清空容器所有元素                                             |
|            | `at(idx)`/`operator[]`       | 访问索引 `idx` 处的元素（`at` 会做越界检查，`[]` 不检查）    |
|            | `resize(n)`                  | 调整容器大小为 `n`，不足补默认值，超出则截断                 |
|            | `begin()`/`end()`            | 返回指向容器首/尾的迭代器（尾迭代器指向最后一个元素的下一位） |
| **list**   | `push_front(val)`            | 在容器头部添加元素 `val`                                     |
|            | `push_back(val)`             | 在容器尾部添加元素 `val`                                     |
|            | `pop_front()`                | 删除容器头部元素                                             |
|            | `pop_back()`                 | 删除容器尾部元素                                             |
|            | `size()`/`empty()`/`clear()` | 同 vector 对应函数                                           |
|            | `sort()`                     | 对容器内元素排序（list 不支持 std::sort，需用自身 sort 成员） |
|            | `remove(val)`                | 删除所有值为 `val` 的元素                                    |
| **map**    | `insert({key, val})`         | 插入键值对（key 唯一，重复插入会失败）                       |
|            | `operator[](key)`            | 访问/插入 key 对应的 val（key 不存在则插入默认值的键值对）   |
|            | `find(key)`                  | 查找 key，返回指向该键值对的迭代器；未找到则返回 end()       |
|            | `erase(key)`                 | 删除 key 对应的键值对                                        |
|            | `size()`/`empty()`/`clear()` | 同 vector 对应函数                                           |
|            | `begin()`/`end()`            | 返回指向首/尾的迭代器（map 迭代器指向的是键值对 pair<key, val>） |
| **set**    | `insert(val)`                | 插入元素 `val`（值唯一，重复插入失败）                       |
|            | `find(val)`                  | 查找 val，返回指向该元素的迭代器；未找到则返回 end()         |
|            | `erase(val)`                 | 删除值为 `val` 的元素                                        |
|            | `size()`/`empty()`/`clear()` | 同 vector 对应函数                                           |
| **queue**  | `push(val)`                  | 在队列尾部添加元素 `val`                                     |
|            | `pop()`                      | 删除队列头部元素（无返回值）                                 |
|            | `front()`                    | 返回队列头部元素的引用                                       |
|            | `back()`                     | 返回队列尾部元素的引用                                       |
|            | `size()`/`empty()`           | 同 vector 对应函数                                           |
| **stack**  | `push(val)`                  | 将元素 `val` 压入栈顶                                        |
|            | `pop()`                      | 弹出栈顶元素（无返回值）                                     |
|            | `top()`                      | 返回栈顶元素的引用                                           |
|            | `size()`/`empty()`           | 同 vector 对应函数                                           |

### 表格2：STL 常用算法函数（需包含 `<algorithm>` 头文件）
| 算法函数                              | 函数作用                                                     |
| ------------------------------------- | ------------------------------------------------------------ |
| `sort(begin, end)`                    | 对 [begin, end) 区间内的元素升序排序（默认 < 比较，可自定义比较规则） |
| `sort(begin, end, cmp)`               | 按自定义比较函数 `cmp` 排序（如 `greater<int>()` 实现降序）  |
| `find(begin, end, val)`               | 在 [begin, end) 区间查找 `val`，找到返回指向该元素的迭代器，否则返回 end() |
| `copy(b1, e1, b2)`                    | 将 [b1, e1) 区间的元素复制到以 b2 为起始的目标区间           |
| `swap(a, b)`                          | 交换两个变量/容器的内容（如 `swap(vec1, vec2)` 交换两个 vector） |
| `reverse(begin, end)`                 | 反转 [begin, end) 区间内的元素顺序                           |
| `count(begin, end, val)`              | 统计 [begin, end) 区间内值为 `val` 的元素个数                |
| `for_each(begin, end, f)`             | 对 [begin, end) 区间每个元素执行函数/函数对象 `f`（如遍历打印元素） |
| `max_element(b,e)`/`min_element(b,e)` | 返回 [b,e) 区间内最大/最小值的迭代器                         |
| `replace(b,e, old, new)`              | 将 [b,e) 区间内所有值为 `old` 的元素替换为 `new`             |
| `unique(b,e)`                         | 去除 [b,e) 区间内连续重复的元素（需先排序，否则只去连续重复），返回去重后尾迭代器 |

### 表格3：STL 常用函数对象（需包含 `<functional>` 头文件）
| 函数对象类型         | 作用说明                                                     | 示例场景                                                    |
| -------------------- | ------------------------------------------------------------ | ----------------------------------------------------------- |
| `plus<T>()`          | 加法函数对象：`plus<int>()(a,b)` 等价于 `a + b`              | 自定义排序、数值运算                                        |
| `minus<T>()`         | 减法函数对象：`minus<int>()(a,b)` 等价于 `a - b`             | 数值运算                                                    |
| `multiplies<T>()`    | 乘法函数对象：`multiplies<int>()(a,b)` 等价于 `a * b`        | 数值运算                                                    |
| `divides<T>()`       | 除法函数对象：`divides<int>()(a,b)` 等价于 `a / b`           | 数值运算                                                    |
| `greater<T>()`       | 大于比较：`greater<int>()(a,b)` 等价于 `a > b`               | sort 降序排序（`sort(v.begin(), v.end(), greater<int>())`） |
| `less<T>()`          | 小于比较：`less<int>()(a,b)` 等价于 `a < b`（sort 默认用此规则） | sort 升序排序（默认）                                       |
| `greater_equal<T>()` | 大于等于比较：`greater_equal<int>()(a,b)` 等价于 `a >= b`    | 自定义比较规则                                              |
| `less_equal<T>()`    | 小于等于比较：`less_equal<int>()(a,b)` 等价于 `a <= b`       | 自定义比较规则                                              |
| `equal_to<T>()`      | 等于比较：`equal_to<int>()(a,b)` 等价于 `a == b`             | 查找、替换的判断条件                                        |
| `not_equal_to<T>()`  | 不等于比较：`not_equal_to<int>()(a,b)` 等价于 `a != b`       | 自定义过滤条件                                              |
| `logical_and<T>()`   | 逻辑与：`logical_and<bool>()(a,b)` 等价于 `a && b`           | 多条件判断                                                  |
| `logical_or<T>()`    | 逻辑或：`logical_or<bool>()(a,b)` 等价于 `a || b`            | 多条件判断                                                  |
| `logical_not<T>()`   | 逻辑非：`logical_not<bool>()(a)` 等价于 `!a`                 | 取反判断                                                    |

### 总结
1. 容器成员函数：不同容器的核心函数围绕“增删查改、状态查询、迭代器获取”展开，需注意特殊容器（如 list 的 sort、map 的键值对访问）的独有接口；
2. STL 算法：核心是对“区间 [begin, end)” 操作，sort、find、for_each 是高频使用的基础算法，使用前需确保区间有效；
3. 函数对象：主要用于自定义比较/运算规则（如 sort 的排序方式），替代手写匿名函数，提升代码复用性和可读性。

## CMake

[CMake 保姆级教程（上） | 爱编程的大丙](https://subingwen.cn/cmake/CMake-primer/)

根据参考文章，按 **功能分类** 整理CMake核心操作指令，涵盖基础配置、变量操作、文件处理、库构建、链接设置等核心场景，表格包含功能分类、指令名称、简介作用、命令语句示例及详细解释，方便快速查阅使用：

| 功能分类            | 指令名称                | 简介作用                                        |                         命令语句示例                         |                             解释                             |
| ------------------- | ----------------------- | ----------------------------------------------- | :----------------------------------------------------------: | :----------------------------------------------------------: |
| 基础配置            | cmake_minimum_required  | 指定CMake最低版本要求，避免版本兼容警告         |            `cmake_minimum_required(VERSION 3.0)`             | 可选配置，未指定可能出现警告，需根据实际安装版本调整（如3.10、3.16） |
|                     | project                 | 定义工程名称，可指定版本、描述、支持语言等      | `project(CALC VERSION 1.0 DESCRIPTION "Calculator Project")` |   核心指令，默认支持所有语言，工程名称与可执行程序名无关联   |
|                     | add_executable          | 生成可执行程序，关联源文件                      |           `add_executable(app add.c main.c;sub.c)`           | 第一个参数为可执行程序名，后续为源文件（空格或分号分隔均可） |
| 变量操作            | set                     | 定义变量（存储源文件列表、路径、参数等）        | `set(SRC_LIST add.c div.c;mult.c)` `set(CMAKE_CXX_STANDARD 11)` | 可用于指定C++标准（如11/17）、路径等，变量通过${变量名}引用  |
|                     | list                    | 列表操作（追加、移除、排序等）                  | `list(APPEND SRC_LIST sub.c)` `list(REMOVE_ITEM SRC_LIST main.cpp)` | 支持APPEND（追加元素）、REMOVE_ITEM（移除指定元素）、SORT（排序）等子命令 |
| 文件搜索            | aux_source_directory    | 搜索指定目录下所有源文件，存储到变量            | `aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR}/src SRC_LIST)` |       第一个参数为搜索目录，第二个参数为存储结果的变量       |
|                     | file(GLOB/GLOB_RECURSE) | 搜索指定路径下符合条件的文件（支持递归）        | `file(GLOB SRC_LIST src/*.cpp)` `file(GLOB_RECURSE HEAD_LIST include/*.h)` | GLOB非递归搜索，GLOB_RECURSE递归搜索，支持通配符（如*.cpp、*.h） |
| 头文件/路径配置     | include_directories     | 指定头文件搜索目录，确保编译器找到头文件        |     `include_directories(${PROJECT_SOURCE_DIR}/include)`     | 参数为头文件目录路径，PROJECT_SOURCE_DIR为工程根目录预定义宏 |
|                     | link_directories        | 指定库文件（静态/动态库）搜索目录               |        `link_directories(${PROJECT_SOURCE_DIR}/lib)`         |            用于链接非系统库时，指定库文件所在路径            |
| 库构建（静态/动态） | add_library             | 生成静态库或动态库                              | 静态库：`add_library(calc STATIC src/*.cpp)` 动态库：`add_library(calc SHARED src/*.cpp)` | STATIC生成静态库（Linux下为.libcalc.a），SHARED生成动态库（Linux下为libcalc.so） |
| 库链接设置          | link_libraries          | 全局链接库，作用于后续所有目标（可执行程序/库） |                `link_libraries(calc pthread)`                | 可链接静态库（掐头去尾名如calc）或系统库（如pthread），全局生效 |
|                     | target_link_libraries   | 为指定目标（可执行程序/库）链接库，支持权限控制 |      `target_link_libraries(app PRIVATE calc pthread)`       | 第一个参数为目标名，PRIVATE/PUBLIC/INTERFACE控制依赖传递，推荐优先使用 |
| 输出路径控制        | EXECUTABLE_OUTPUT_PATH  | 指定可执行程序输出路径                          |   `set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)`    |         路径不存在时自动创建，./对应Makefile所在目录         |
|                     | LIBRARY_OUTPUT_PATH     | 指定静态库/动态库输出路径（通用）               |     `set(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)`     | 适用于静态库（无执行权限）和动态库，比EXECUTABLE_OUTPUT_PATH通用 |
| 日志输出            | message                 | 输出日志信息（支持不同级别）                    | `message(STATUS "Source Dir: ${PROJECT_SOURCE_DIR}")` `message(WARNING "Warning!")` | 级别包括STATUS（普通）、WARNING（警告）、FATAL_ERROR（致命错误，终止构建） |
| 宏定义              | add_definitions         | 向编译器添加宏定义（类似gcc -D）                |  `add_definitions(-DDEBUG)` `add_definitions(-DNUMBER=10)`   |     等价于在代码中定义#define DEBUG，可用于控制条件编译      |

**补充说明**

1. 预定义宏：文章中提到的常用预定义宏（如PROJECT_SOURCE_DIR、CMAKE_CURRENT_SOURCE_DIR）无需手动定义，可直接引用，用于获取工程根目录、当前CMakeLists.txt所在目录等；
2. 命令大小写：CMake支持大小写混合（如Cmake_minimum_required、PROJECT），但建议统一风格（如全小写或全大写）；
3. 推荐用法：复杂项目优先使用`target_link_libraries`（精准控制目标依赖）、`file(GLOB_RECURSE)`（批量搜索文件）、`out-of-source`构建（单独build目录，避免污染源码）。

## Debug

[gdb 调试入门（一）：Windows/Linux/Ubuntu 下安装 gdb_windows gdb安装-CSDN博客](https://blog.csdn.net/zztiger123/article/details/105540489)

## GDB

根据参考文章中的GDB指令集汇总，整理出**命令全称、缩写、含义说明**的对照表，覆盖文章中所有高频使用的调试命令，确保信息与原文一致：

| 命令全称            | 缩写      | 含义说明                                                     |
| ------------------- | --------- | ------------------------------------------------------------ |
| list                | l         | 显示对应代码，每次默认显示10行；可接行号（如`l 1`从第1行开始）或函数名（如`l Addtop`） |
| run                 | r         | 运行程序（无断点直接执行完毕，有断点则从第一个断点开始运行，等价于VS的F5） |
| breakpoint          | b         | 设置断点：<br>1. `b 行号`（指定行打断点）；<br>2. `b 源文件:函数名`（函数第一行打断点）；<br>3. `b 源文件:行号`（指定文件的指定行打断点） |
| info breakpoints    | info b    | 查看所有断点信息，包括编号、地址、是否可用、命中次数等       |
| delete              | d         | 删除断点：<br>1. `d 断点编号`（删除指定断点，不可用行号）；<br>2. `d breakpoints`（删除所有断点，需确认y/n） |
| disable breakpoints | disable b | 禁用断点：<br>1. `disable b`（禁用所有断点）；<br>2. `disable b 编号`（禁用指定断点） |
| enable breakpoints  | enable b  | 启用断点：<br>1. `enable b`（启用所有断点）；<br>2. `enable b 编号`（启用指定断点） |
| next                | n         | 逐过程调试（等价于VS的F10，不进入函数内部，单步执行）        |
| step                | s         | 逐语句调试（等价于VS的F11，进入函数内部，包括库函数，单步执行） |
| backtrace           | bt        | 查看函数调用栈（显示程序执行到当前位置的函数压栈过程）       |
| set var             | -         | 调试过程中修改变量的值（如`set var i=9`，直接修改变量i为9）  |
| print               | p         | 打印指定变量的当前值（如`p count`查看变量count的值）         |
| display             | -         | 跟踪查看变量，每次程序暂停时自动显示该变量的值（支持变量、结构体、地址等，如`display &i`跟踪变量i的地址） |
| undisplay           | -         | 取消变量跟踪，需接跟踪变量的编号（如`undisplay 3`取消编号为3的跟踪项） |
| until               | -         | 指定行号跳转，执行完当前位置到目标行之间的代码（如`until 13`直接跳转到第13行，结束中间循环/代码块） |
| finish              | -         | 在函数内部执行，直到当前函数执行完毕并返回，然后暂停（快速跳出函数，无需逐句调试） |
| continue            | c         | 从当前断点继续运行，直到遇到下一个断点或程序结束（等价于VS中反复按F5） |
| quit                | q         | 退出GDB调试环境                                              |

watch point 查看变化的量